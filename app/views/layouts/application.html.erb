<!DOCTYPE html>
<html>
  <head>
  <!-- Google tag (gtag.js) -->
  <script async src='https://www.googletagmanager.com/gtag/js?id=G-SRZQ9JM67Y'></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
  
    gtag('config', 'G-SRZQ9JM67Y');
  </script>
    <title>Where Does Back Pain Come From</title>
    <%= favicon_link_tag 'スライド1.png'%>
    <meta name='viewport' content='width=device-width,initial-scale=1'>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- OGP設定 -->
    <%= csp_meta_tag %>
    <% set_meta_tags(default_meta_tags) %>
    <%= display_meta_tags %>
    <meta property='og:title' content='Where Does Back Pain Come From'>
    <meta property='og:description' content='このアプリは腰痛を簡易的にタイプ別に分け、セルフケア方法をお伝えするアプリです。'>
    <meta property='og:image' content='<%= asset_path('top_page.png') %>'>
    <meta property='og:type' content='website'>
    <meta property='og:url' content='<%= request.original_url %>'>

    <!-- Bootstrap CSSを読み込み -->
   <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css', crossorigin: 'anonymous' %>
   <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js', crossorigin: 'anonymous' %>


    <!-- 全ページで application.css を読み込み -->
      <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>

  </head>

  <body>
    <!-- ナビゲーションバー -->
    <nav class='navbar navbar-expand-lg navbar-custom'>
      <div class='container-fluid'>
      <a class='navbar-brand' href='<%= root_path %>'>
      <%= image_tag 'navbar_logo.png', alt: 'ロゴ', class: 'navbar-logo' %>
    </a>

        <!-- トグルボタン（モバイル用） -->
        <button class='navbar-toggler' type='button' data-bs-toggle='collapse' data-bs-target='#navbarSupportedContent' aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation'>
          <span class='navbar-toggler-icon'></span>
        </button>
        
        <!-- ナビゲーションメニュー -->
        <div class='collapse navbar-collapse' id='navbarSupportedContent'>
          <ul class='navbar-nav ms-auto'>
            <% if user_signed_in? %>
              <li class='nav-item'>
                <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'btn btn-danger', data: { confirm: '本当にログアウトしますか?' } %>
              </li>
              <li class='nav-item'>
              <%= link_to '退会する', cancel_user_registration_path, class: 'btn btn-danger', data: { confirm: '本当に退会しますか?' } %>
              </li>
            <% else %>
              <li class='nav-item'>
                <%= link_to 'ログイン', new_user_session_path, class: 'nav-link' %>
              </li>
              <li class='nav-item'>
                <%= link_to '新規登録', new_user_registration_path, class: 'nav-link' %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- フラッシュメッセージを表示 -->
    <% if flash[:alert] %>
      <div class='alert alert-danger'>
        <%= flash[:alert] %>
      </div>
    <% end %>

    <% if flash[:notice] %>
      <div class='alert alert-success'>
        <%= flash[:notice] %>
      </div>
    <% end %>

    <!-- ここで各ページのコンテンツを表示 -->
    <%= yield %>

    <!-- Bootstrap JavaScript（Popper.jsを含むバンドル版） -->

    <!-- Importmap Tags -->
    <%= javascript_importmap_tags %>

  </body>
  <footer class="footer">
  <ul>
    <li><a href='<%= terms_of_use_path %>'>利用規約</a></li>
    <li><a href='<%= privacy_path %>'>プライバシーポリシー</a></li>
  </ul>
</footer>

</html>
