<!DOCTYPE html>
<html>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>

  <head>
    <title>where-does-back-pain-come-from</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSSを読み込み -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css", integrity: "sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+3mgCxU8ksdQRVvoxMfooAOXYBexz", crossorigin: "anonymous" %>

    <!-- 全ページで application.css を読み込み -->
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  </head>

  <body>
    <!-- ナビゲーションバー -->
    <nav class="navbar navbar-expand-lg navbar-custom">
      <div class="container-fluid">
        <!-- トップページへのリンク -->
        <a class="navbar-brand" href="<%= root_path %>">where does back pain come from</a>

        <!-- トグルボタン（モバイル用） -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'btn btn-danger', data: { confirm: "本当にログアウトしますか?" } %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "ログイン", new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "新規登録", new_user_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- ここで各ページのコンテンツを表示 -->
    <%= yield %>

    <!-- Bootstrap JavaScript -->
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js", integrity: "sha384-qVjmWBw3Jr7e3VoPf+P2xx6qfLRpBFeTujwHG2aWnB3lRmy8lue3tEmf6svYENNp", crossorigin: "anonymous" %>


    <!-- Importmap Tags -->
    <%= javascript_importmap_tags %>

  </body>
</html>
